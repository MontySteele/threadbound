# S21 status — The New Canon (implementation record)

Sprint doc: `docs/threadbound_sprint_S21_the_new_canon.md` (charter and
D-list live there; this file is the execution record). Designer kickoff
2026-07-07: "if there are no open questions, let's get to it" — D0–D5
taken as ratified as recommended per the S18/S19/S20 precedent, with
TWO in-flight designer rulings taken by question this session:

1. **The gravebloom g-row → g-a** (the detonate-side rider). The only
   sprint row with no recommendation attached (S18 stalled it as
   "enumerated only"); asked before work began, per the kickoff's own
   framing.
2. **St-h1 → the solo qualifier.** The pre-screen found the proposed
   sentence's machinery (S19.6 Witness pointers) is SOLO-only — co-op
   first-timers are taught by the First-Chain overlay, which the reword
   stopped mentioning. Ruled: "Descending alone? The Witness offers a
   few pointers during each run's first act — Links, the Thread, and
   the leash explain themselves as they come up."

Fresh-container note (instrument law): `npm install` was required
first; both parity instruments then reproduced their banks
byte-identically on the tip (s19 + s20 scripts) and the suite read
431/431 before any change landed.

## Part 1 — S21.1 the OQ#69 ruling (landed first, as charted)

`computeLinksFired` aligns on `grownDef` — the S20.1 alignment
completed: legality, preview, resolution, and natural fire read the
same card. The fired-boolean changes ONLY where growth changes link
EXISTENCE — exactly the two mutated echo forms (First-Drawn Descant
past 6 resonances, First-Lit Votive past 20 Thread spent); base
growers' tier links keep their conditions, as the sprint doc predicted.

Paired same-seed battery (S16-R1 form, n=500 ×3, seeds 20001–20500):

| leg | wins | discordant (up/down) | changed runs | attributed |
|---|---|---|---|---|
| vv | 333→334 | 5 (3/2) | 33 | 33/33 rite_descant/votive plays |
| vb | 338→341 | 9 (6/3) | 74 | 74/74 |
| bb | 380→383 | 13 (9/4) | 102 | 102/102 |

Zero non-grower movement — the stop-and-report trigger never fired.
The bb > vb > vv ordering is the mechanism's own signature (Descant's
6-resonance tier is the easy one and bram seats carry it). Regression
test pins the tier-granted echo link both directions
(`s21-natural-grown-link.test.ts`). s19 archaeology PASS byte-identical
(the rites-off canon provably cannot see the change); s20 re-banked
LOUD in-commit (the S20-R1 convention).

## Part 2 — S21.2 instruments + the OQ#70 closure (D2 ratified)

**Instruments first (own commit):** act 2/3 HP-lost/combat, the
act-death profile, act-3 lethality (% of act-3 arrivals lost), and the
OQ#68 knot-contact watch join the canonical report; both parity diffs
were exactly the four summary lines, shown at re-bank.
`aggregate-human.mjs` mirrors them — the 2f filing routes the FIRST
HUMAN lethality read through that aggregator, so the line exists where
it will be read; its stale 25–45 hex gate and S14-R1 win label retired
with the ruling (test pin updated).

**The S21-R1 anchor board** (`docs/reference/s21-p2000-bank.txt`):
n=2000 ×3, seeds 20001–22000, run twice — byte-identical across
invocations on all three legs. vv 66.9 / vb 69.6 / bb 76.4; paired
attribution to the S21.1 ruled event on record (−0.2/−0.15/+0.2 pts vs
S20-R1). The band set 2a–2f recorded in OPEN-QUESTIONS as the OQ#70
closure; the 2b/2c/2d wires are encoded in sim.ts and BIND only at the
canonical board form (n=2000 same-seed braid A0 base config) — every
other battery stays REPORTED with the anchor printed.

## Part 3 — S21.3 the ladder survey (D3 ratified as written)

18 cells, n=500/cell, paired seeds, banked with per-cell shas:
`docs/reference/s21-ladder-survey.txt`. The A0 row reproduces the
S21.1 post battery exactly (334/341/383) — the free cross-check.

| finding | read |
|---|---|
| the curve | S18-era A2 cliff (~17) NOT real on this canon — A2 reads 43–51; a wall (−13..−21 from A1), not a cliff |
| monotone? | NO: A2→A3 RISES everywhere; A3→A4 flat everywhere |
| the A3 question | inversion REAL and UNIVERSAL: +3.6/+2.2/+3.2, paired per-seed net +18/+11/+16 of 500, knot fights/run rising in step |
| the A4/A5 tail | A4 is a bot NO-OP (±0.6 — bots don't fray, OQ#64; unreadable by this instrument, REPORTED). A5 monotone and toothy (−8.6/−18.6/−16.6), act-3 lethality ~doubles |

## Part 4 — S21.4 recomposition (evidence-gated; the survey demanded exactly one lever)

**A3-a EXECUTED** (D4's recommendation, fired by the survey): the A3
extra crossing (`CROSSING_LAYER_A3` — generated only when the
extraElite mod is live) pays NO card picks. The rung's elite is a
toll, not a knot: S16-D3's both-seat picks stay scoped to the base
crossings; relic, covet charge, gold, and the bound-witness fragment
untouched. Pre-answered picks are the covet-cache pass-through
precedent. A0–A2 reward flow and rng are byte-identical BY
CONSTRUCTION (no layer-5 elite exists below A3) — parity PASS
unchanged, s16-knot-reward pins hold, and the post-change A2 legs
reproduce the survey's A2 run rows byte-for-byte (see the battery
below). Pins: `s21-a3-toll.test.ts` (toll pays no picks + screen is
pass-through; base crossings still pay at A3; no toll exists below A3).

**A2-b as recommended, trivially:** the cliff the lever was enumerated
for does not exist on this canon — nothing to soften, the read is
filed (OQ#66 closure).

**A4: no ruling** — the survey shows a bot blindspot, not a shape;
none of the pre-enumerated levers address "the instrument cannot read
the rung," and inventing one mid-pass is what Part 4 forbids. The A4
human read files with the fray-relic pile (OQ#64).

Post-toll paired battery (same seeds as the survey, A2–A5 ×3):

| cell | pre → post win% | note |
|---|---|---|
| A2 all three | BYTE-IDENTICAL run rows | the scoping proof |
| A3 vv | 54.2 → 52.2 (paired net vs A2: +8, was +18) | |
| A3 vb | 45.6 → 42.8 (net −3, was +11) | |
| A3 bb | 46.6 → 44.8 (net +7, was +16) | |
| A4 vv | 54.4 → 52.8 | |
| A4 vb | 45.6 → 42.8 | |
| A4 bb | 46.0 → 44.6 | |
| A5 vv | 45.8 → 42.8 | |
| A5 vb | 27.0 → 25.4 | |
| A5 bb | 29.4 → 28.6 | |

**After-read:** the universal inversion is GONE — post-toll A3 vs A2
paired nets read +8/−3/+7 of 500 (mixed sign, inside per-cell noise;
they were +18/+11/+16, one sign). The residual is exactly what the
lever kept by its own words (the toll still pays relic, covet charge,
and gold): whether that remainder should also go is a question for the
HUMAN ladder read, not another bot-window dose — filed with the OQ#66
closure. A4/A5 shift down in step (the toll rides every extraElite
rung), tail shape unchanged.

**Rung copy (the owed retext row):** rung 3 → "one more elite per act
— a toll: its crossing pays no card picks". Landed PROVISIONAL; the
signature row is Part 5 St-a1 below. The ratified A4 string untouched.

## Part 5 — S21.5 riders (D5)

1. **St rows signed.** Pre-screen verified against rites.ts — all
   eight growth rows match axes/rates/caps/tiers exactly (Shroud
   falls/2/8; Vigil boundKills/1/9 per-seat; Knell detonations÷3/12;
   Pyre-Brand kindledConsumed÷6/8; Mourner's momentumSpent÷15/6; Toll
   linksFired÷25/2; Votive tiers 8/20 threadSpent; Descant tiers 6/14
   resonances). **St-e1, St-r1..r8, St-m1: SIGNED as proposed.**
   **St-h1: REWORDED + SIGNED with the solo qualifier** (designer
   ruling this session; the mode-gap finding above).
2. **Del-1..3 signed and executed** (comfort-pass.patch,
   spotcheck-s1s2.js, sim-shard.sh) — grep proofs re-run and recorded
   in the commit; suite green before and after.
3. **gravebloom g-a** (designer ruling this session): the detonate-side
   rider — EXECUTED. A second hook —
   "the first Detonation each turn: apply 2 Hex to ALL" — inside the
   S13.2 law (flat, ≤2, per-turn-capped; independent charge from the
   identity hook; covenant CI checks each). Paired battery n=500 A0:
   vv 334→334, vb 341→339, every changed run (10/21) attributes to a
   gravebloom pick; bb byte-identical at n=200 (no vess seat). BOTH
   parity banks re-banked with this attribution (the card predates
   rites, so the s19 archaeology era sees it too — its diff's changed
   runs all carry gravebloom plays). Bots barely value the card either
   way; the g-a read is a human-table question, which is why the axis
   was ruled, not dosed. Pins: s21-gravebloom-ga.test.ts.
4. **OQ#65 EXECUTED: the lane generator is deleted.** Own commit; both
   parity instruments byte-identical after with NO re-bank — the braid
   canon provably cannot see it. Suite audit dispositions (all loud, in
   the commit and the OQ#65 ledger entry): the S11.1 composition-CI
   family (src/map-composition.ts + its test + scripts/
   map-composition.js) measured only the deleted generator and left
   with it — the braid's own CI lives in s11-braid.test.ts; the
   tracks-covenant golden regenerated in-commit (flag-off runs are
   braid runs now); the S16-D7 anti-streak pin re-pinned to the
   mechanism's real invariant (no 3-streak — strict alternation at a
   tied counter was lane-rng luck); the wrong-way clue:normal ceiling
   re-banked descriptively 2.9→4.5 (the braid's truth strand is
   clue-scaled BY DESIGN, S11.11-4); lane rows dropped from the
   elite-sampling/pierce sweeps; the three lifecycle pins keep their
   transport coverage on the braid as the ruling worded it.
   TB_KNOTWORK is dead env; README carries "the lane is gone; the
   braid is the game."
5. **Deployed smoke run + `resonance_together` exhaustion read: STILL
   OPEN.** This session never touched the hosted build, and the gate is
   the designer's eye — it rides the next session that does, as the
   sprint doc says.

### Strings sign-off table

| # | surface | text | status |
|---|---|---|---|
| St-e1 | title epigraph | "Two go down together, bound by one thread." | **SIGNED** |
| St-h1 | How-to overlay | "Descending alone? The Witness offers a few pointers during each run's first act — Links, the Thread, and the leash explain themselves as they come up." | **SIGNED (reworded + solo qualifier, designer ruling 2026-07-07)** |
| St-r1..r8 | vestry growth lines/ladders | as landed at S20 | **SIGNED** (pre-screen record above) |
| St-m1 | map captions | none new | **SIGNED** (clause reads answered) |
| St-a1 | rung-3 lobby copy | "one more elite per act — a toll: its crossing pays no card picks" | PROVISIONAL — the A3-a retext row; needs signature |
| St-g1 | gravebloom card text | "Power: the first time your partner’s card fires a Link each turn, apply 2 Hex to ALL enemies. The first Detonation each turn: the same. Exhaust." | PROVISIONAL — the g-a retext row; needs signature |

## Part 6 — exit gates

1. **Suite green:** 430/430 (431 at entry + S21.1's two pins +
   S21.4's three − the nine lane-only rows retired by the OQ#65
   audit). **Parity:** byte-identical through Part 1's landing; the
   Part 1 change itself re-banked LOUD with its paired attribution
   (S20-R1 convention); the Part 2 instrument/label diffs shown and
   re-banked in their commits; the lane deletion and A3-a changed
   NOTHING (PASS unchanged, no re-bank — proven, not assumed).
2. **Part 2 bands** recorded in OPEN-QUESTIONS as the OQ#70 closure;
   act-3 lethality in the canonical report from this sprint forward
   (and mirrored in the human aggregator, where the 2f question will
   actually be read).
3. **Part 3 grid** complete and banked (docs/reference/), per-cell
   rows in this doc's survey table and the bank.
4. **Part 4:** one ruling (A3-a), landed with its paired battery
   attribution and its copy row in the sign-off table; A2-b's "no
   ruling" is the survey being the deliverable, as written.
5. **Riders executed exactly;** the two rows that could not close this
   session say so loudly (deployed smoke run — needs the hosted build
   and the designer's eye; St-a1/St-g1 signatures — new strings born of
   this session's own rulings).
6. **No-balance audit:** every number change traces to a D-row — the
   S21.1 alignment (D1), the A3-a toll (D4, survey-gated), gravebloom
   g-a (D5, designer-ruled this session). Balance commits separate
   from content/strings commits throughout; 2f explicitly NOT re-dosed.

## Part 7 — D-list disposition

| D | ruling | disposition |
|---|---|---|
| D0 | ratified | Parts 1→2→3→4 in order, riders after 1, own commits throughout |
| D1 | **align** executed | paired battery clean (zero non-grower movement); LOUD re-bank; both-directions regression pin |
| D2 | ratified as proposed | 2a–2f recorded; wires bind at the canonical board only; 2f filed to the first human read |
| D3 | ratified | 18 cells banked with shas; A0 row cross-checks the Part 1 battery |
| D4 | **A3-a fired** (survey-gated); A2-b moot (no cliff); A4 no-ruling (bot blindspot) | toll landed with attribution + copy row; nothing invented mid-pass |
| D5 | signed / ruled | St rows signed (h1 reworded by ruling); Del executed; gravebloom g-a; OQ#65 deletion executed with the suite audit on the record |

**After close:** the handoff package note stands (LAWS.md, the
two-human protocol, this doc's successor notes) — the S22 design pass
was NOT drafted this session; it needs the designer's charter, and the
toll's first human read belongs in it.
